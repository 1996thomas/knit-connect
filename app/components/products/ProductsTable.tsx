import { Card, DataTable, LegacyCard, Text } from "@shopify/polaris";
import { Product } from "app/types/products";
import React from "react";

export default function ProductsTable({ products }: { products: Product[] }) {
  return <Card></Card>;
}
